# Backend Environment Variables

# Mock Server Configuration
# Port for the mock server (default: 3000)
PORT=3000

# Note: The mock server uses in-memory mock data and does not require
# database connection strings. For production AWS deployment, DynamoDB
# table names are configured through CDK infrastructure.

# AWS Configuration (for production deployment only, not needed for local mock server)
# STUDENTS_TABLE_NAME=ho-yu-students
# TEACHERS_TABLE_NAME=ho-yu-teachers
# GAMES_TABLE_NAME=ho-yu-games
